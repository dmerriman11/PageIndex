.ipynb_checkpoints
__pycache__
.DS_Store
.env*
.venv/
logs/
workspace
pb_data
pb_migrations

# graft's local repository settings — not committed.
/.graft/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Local workspace archives (can be GB-sized; never commit)
workspace.zip
