.ipynb_checkpoints
__pycache__
.DS_Store
.env*
.venv/
logs/
workspace
workspace.zip
pb_data
pb_migrations

# graft's local repository settings — not committed.
/.graft/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Local workspace archives (can be GB-sized; never commit)
workspace.zip
venv/
# Local model weights (e.g. the optional BGE re-ranker, ~280 MB) - never committed.
/models/
